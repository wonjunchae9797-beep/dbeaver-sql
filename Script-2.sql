/*
      < SELECT >
      데이터를 조회하거나 검색할 때 사용하는 명령어
      
     SELECT
            조회하고자 하는 컬럼
            조회하고자 하는 컬럼
          ,
       FROM   
            테이블명;
     - ResultSet : SELECT문을 통해 조회된 데이터 결과물
*/

-- ANIMAL 테이블에서 모든 컬럼을 전부 다 조회 new String("abc")

	CREATE USER C##SC IDENTIFIED BY SC;
	GRANT CONNECT, RESOURCE TO C##SC;
	GRANT CREATE SESSION TO C##SC;